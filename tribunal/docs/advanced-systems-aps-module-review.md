# Advanced Systems APS Eden and Zeus activation review

## Scope and result

This review covers the public Eden `YAS_APS_Module` and curator-visible
`YAS_APS_Zeus_Toggle_Module`: configured dispatch, exact target selection,
authority, result, and logic lifecycle. Accepted APS projectile interception,
resources, replication, and lifecycle-tail behavior remain unchanged.

**Split classification:**

* Eden synchronized enable: **REFINED; ACCEPTED / COVERED**.
* Zeus per-vehicle toggle: **REVIEWED / REFINE BEFORE PERMANENT COVERAGE**.

Fresh run `20260821T132642Z-925c4208` proves the authentic typed Eden path. Two native modules dispatched on the server, retained their native Sync links, and independently enabled their exact synchronized vehicles. A forged client call reached the handler and was rejected. Two exact threats were hard-killed while the same unsynchronized control was physically damaged. The real curator path remains unmeasured and is not accepted.

## Stable Eden contract and candidate Zeus contract

A genuine typed Eden module installs APS on every exact valid synchronized vehicle; multiple modules aggregate. The non-disposable native module and Sync relations remain available. A genuine authorized curator module should toggle exactly one selected vehicle once. The server validates module identity, requester, target, current
transition, and replay; returns a correlated truthful result; and consumes only
its own disposable logic. Invalid, ambiguous, unauthorized, stale, or replayed
requests change no vehicle/resource and delete no unrelated object.

## Canonical twelve-question review

1. **User or integrator observation.** Mission makers synchronize vehicles to
   the Eden module to install APS at mission start. An authorized curator places
   the Zeus module on one vehicle to toggle APS. The observation is authentic
   activation of accepted APS behavior, not another APS implementation.
2. **Current behavior and negative paths.** Native Eden dispatch executes on the server with local owner-2 logic and `remoteExecutedOwner=0`. The refined handler accepts only that exact typed/local path, enables synchronized `AllVehicles`, and retains non-disposable logic. Multiple modules aggregate. Zeus still forwards, selects the first synchronized object or `attachedTo`, toggles, emits broad feedback, and deletes its logic; its authority and ambiguity remain unresolved.
3. **Authority, locality, and lifecycle.** Eden mutation is server-owned and now validates exact class, server locality/owner, and native dispatch. A client-owned soldier call (`owner=4`, `remoteExecutedOwner=4`) was recorded and rejected without mutation. Eden logic and exact Sync relations replicated to client-a and remained until scenario cleanup. Zeus requester, replay, result, and cleanup authority remain unproven.
4. **Generic mechanics.** Typed mission-module generation, native SQM Sync
   links, configured-function receipts, execution locality, real curator
   placement, and exact logic cleanup can be product-neutral Tribunal mechanics.
   APS state and combat meaning remain Advanced Systems.
5. **Product-owned behavior.** Advanced Systems owns valid vehicles, aggregate Eden-module behavior, APS installation side effects, authorized curators, exactly-one Zeus selection, placing-curator feedback, and disposable Zeus logic cleanup.
6. **Engine facts.** For this Eden class, `isGlobal=0` configured dispatch ran server-local with owner 2 and `remoteExecutedOwner=0`; non-server forwarding was unnecessary. Native mission Sync links were visible on server and client-a. `vehicleVarName` was still empty at early configured dispatch, so identity evidence uses type, netId, locality, position, and exact synchronized objects. Curator attachment transport remains uncharacterized.
7. **Fragile or incomplete details.** The Eden dispatch audit is bounded to 64 diagnostic rows and is not the causal oracle. Zeus still has ambiguous first-target selection, caller-supplied logic deletion, no authenticated result/replay guard, and broad notification. Toggle must use the already-accepted suspend/resume semantics and must not silently resupply or reset operator choices.
8. **Better native or existing mechanism.** Keep Arma's native module framework
   and the accepted server APS lifecycle tail. Measure authentic transport
   before deciding whether any explicit request route is needed. A validated
   operation endpoint is appropriate only where curator transport requires it.
9. **Stable contract and causal proof.** Eden requires a real typed SQM module
   and Sync link, exact dispatch receipt, exact installed target, and one accepted
   calibrated projectile interception. An identical unsynchronized/no-module
   target must physically impact. Zeus requires real curator placement, exact
   requester/logic/target, OFF-impact then ON-interception, and forged/invalid/
   replay controls with unchanged independent state.
10. **Replaceable details.** Function names, priority, internal variables,
    notification wording, private receipt schema, fixture coordinates, ACE
    registration details, and transport mechanism remain free.
11. **Characterization.** Typed Eden dispatch, locality, Sync identity, persistence, aggregate modules, and physical causality are established. Next characterize one real curator placement. One bounded VNC
    placement is justified only if a programmatic native path cannot be proven
    equivalent; permanent outcomes remain data-driven.
12. **Tribunal promotion.** The first real consumer now proves the narrow validated typed-Logic/Object/SQM-Sync fixture; it never exposes arbitrary raw SQM.
    Curator transport stays scenario-local until a second consumer proves reuse.

## False-PASS audit

Calling either handler or `YOSHI_fnc_apsEnableVehicle`/toggle API directly is a
pipeline bypass. Config presence, enabled variables, charge top-up, ACE actions,
notification, or logic deletion do not prove authentic module activation.
Positive proof needs fresh exact module/vehicle/projectile identities and the
accepted physical APS oracle. The negative must independently prove a valid
projectile impacts. An absent forged effect is meaningful only after an exact
request receipt proves the authority boundary was reached.

Do not include anti-drone in this contract: its activation policy is separately
deferred. Do not claim curator, JIP, or client-N behavior from client-a.

## Evidence and continuation

Fresh autonomous run `20260821T132642Z-925c4208` passed 13 server and 6 client assertions. Projectiles `2:158` and `2:159` produced exact hard-kill ledger events on targets `2:13` and `2:16`, consumed one charge each (`40 -> 39`), terminated before impact, and produced no damage. Unsynchronized target `2:19` received projectile `2:160`: the independently verified path intersected the exact target, closest physical approach was `5.92099 m`, the projectile terminated, exact RPG/penetrator HandleDamage records occurred, and damage rose `0.0454545 -> 0.478509` with no APS event or charge change. Client-a resolved the same vehicles, projectile IDs, and hard-kill events.

The generic typed entity/Sync fixture is now a proven Tribunal mechanism. Remaining work is Zeus only: characterize one real assigned-curator placement, then enforce the selected all-assigned-curators policy, exactly-one target, placing-curator feedback, replay/invalid rejection, and own-disposable-logic cleanup. Run OFF-impact then ON-interception through that authentic entry. Client-B/JIP remains deferred.
